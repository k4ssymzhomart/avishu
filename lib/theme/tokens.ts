import { borders } from '@/lib/theme/borders';
import { colors } from '@/lib/theme/colors';
import { spacing } from '@/lib/theme/spacing';
import { typography } from '@/lib/theme/typography';

export const theme = {
  borders,
  colors,
  spacing,
  typography,
} as const;
